# <Title — short reusable lesson statement>

**Category:** environment — <root cause class: tooling, platform, config, dependency, etc.>
**Tags:** <comma-separated keywords>
**Date:** <YYYY-MM-DD>
**Confidence:** <High | Medium | Low>

## Pattern

The generic environment condition or quirk class — described so it generalizes beyond this setup.

## Reusable Guidance

How to handle it in similar environments. Must stand alone as a generic takeaway.

## Specific Context (optional)

Fill only when the Decision Gate justifies it — i.e. a critical/shared component, unique
design constraints, or non-obvious logic that cannot be represented without exact detail
(e.g. an exact version, platform, or path that the behavior truly depends on). Leave empty otherwise.

## Example (optional)

A brief concrete instance illustrating the quirk and workaround.

## Applicability

Where this lesson should generally apply (which environments, platforms, or setups).
